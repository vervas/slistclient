{% extends 'layout.jinja' %}

{% block title %}List Items{% endblock %}
{% block content %}
    <p>
        <a href='/lists' class="btn btn-primary" type="button">Lists</a>
        <a href='#' class="btn btn-success" type="button">Add Item</a>
    </p>
    <p><ul class="list-group">
        {% for item in items %}
            <li class="list-group-item">
                <a href='#' type="button" class="btn btn-danger">X</a>
                <a href='#'><strong>{{item['name']}}</strong></a>
                <span class="badge">{{item['priority']}}</span>
            </li>
        {% endfor %}
    </ul></p>
{% endblock %}
